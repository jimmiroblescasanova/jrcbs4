require("./bootstrap");

window.moment = require("moment");

import swal from 'sweetalert2';
window.Swal = swal;

/*window.livewire.on("LiveAlert", (event) => {
    swal({
        icon: event.icon,
        title: event.title,
        text: event.message,
    });
});*/
